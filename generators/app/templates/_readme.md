# <%= projectName %>

<%= projectDescription %>

This is a Maven-based Java library project generated by `generator-library-maven`, a Yeoman equivalent of the Bernardo-MG library Maven archetype.

## Features

- Java <%= javaVersion %> Maven library skeleton
- Unit tests and integration tests with JUnit Jupiter, Surefire, and Failsafe
- GitHub Actions CI workflow
- GitHub Packages deployment workflow
- Maven site/reporting setup, including Javadocs, project info, Checkstyle, SpotBugs, and changes report entries
- MIT license, `.gitignore`, `.gitattributes`, and placeholder source/test classes

## Build

```bash
mvn verify
```

## Quality checks

```bash
mvn -Pquality verify
```

## Publish to GitHub Packages

Configure `GITHUB_TOKEN` in GitHub Actions or in your Maven settings, then run the release workflow.

## Project links

- Repository: <%= repoUrl %>
- Issues: <%= repoUrl %>/issues
